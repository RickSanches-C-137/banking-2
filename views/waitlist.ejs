<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Waitlist Email Summary</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      background-color: #f5f9ff;
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
      color: #0b2545;
    }
    .container {
      width: 100%;
      max-width: 600px;
      margin: 40px auto;
      padding: 0 16px;
    }
    h1 {
      text-align: center;
      color: #003a8c;
      margin-bottom: 24px;
    }
    .summary {
      background: #fff;
      padding: 24px;
      border-radius: 8px;
      text-align: center;
      box-shadow: 0 2px 8px rgba(0,0,0,0.05);
    }
    .count {
      font-size: 48px;
      font-weight: bold;
      color: #007bff;
      margin: 16px 0;
    }
    ul {
      text-align: left;
      margin-top: 24px;
      padding-left: 20px;
    }
    li {
      margin-bottom: 6px;
    }
    .footer {
      text-align: center;
      margin-top: 32px;
      font-size: 12px;
      color: #8893a7;
    }
    @media only screen and (max-width: 600px) {
      .count { font-size: 36px; }
      h1 { font-size: 20px; }
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Waitlist Summary</h1>

    <div class="summary">
      <p>Total Emails on Waitlist:</p>
      <div class="count"><%= emails.length %></div>

      <% if (emails.length > 0) { %>
        <p>Emails:</p>
        <ul>
          <% emails.forEach(email => { %>
            <li><%= email %></li>
          <% }) %>
        </ul>
      <% } else { %>
        <p>No emails yet.</p>
      <% } %>
    </div>

    <div class="footer">
      © <%= new Date().getFullYear() %> Bucks. All rights reserved.
    </div>
  </div>
</body>
</html>
